name = "bootstrap-release-qa"
description = "repo-bootstrap 설치/제거/템플릿 변경을 릴리즈 전 관점에서 검증하고 미실행 위험을 정리한다."
model = "default"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = "이 역할은 .harness/docs/team-spec.md의 bootstrap_release_qa 섹션과 공통 출력 블록을 단일 원천으로 따른다. 역할별 우선 입력, 절차, 다음 역할, 종료 기준을 이 TOML에 반복하지 않는다."
