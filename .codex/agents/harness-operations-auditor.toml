name = "harness-operations-auditor"
description = "repo-bootstrap 로컬 하네스의 team-spec, agent/skill 포인터, 오케스트레이션, 로그 정합성을 감사(audit)하고 재진입 Phase를 권고한다."
model = "default"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = "이 역할은 .harness/docs/team-spec.md의 harness_operations_auditor 섹션과 공통 출력 블록을 단일 원천으로 따른다. 역할별 우선 입력, 절차, 다음 역할, 종료 기준을 이 TOML에 반복하지 않는다."
