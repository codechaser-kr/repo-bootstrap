name = "bootstrap-flow-coordinator"
description = "repo-bootstrap 하네스 역할 흐름을 조율하고 설치/템플릿/QA/운영 감사(audit) handoff를 정렬한다."
model = "default"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = "이 역할은 .harness/docs/team-spec.md의 bootstrap_flow_coordinator 섹션과 공통 출력 블록을 단일 원천으로 따른다. 역할별 우선 입력, 절차, 다음 역할, 종료 기준을 이 TOML에 반복하지 않는다."
